{
  "name": "api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "PYTHONPATH=. uv run fastapi dev main.py --host localhost --port 8000",

    "start": "PYTHONPATH=. uv run uvicorn main:app --host 0.0.0.0 --port 8000",
    "install": "uv sync"
  },
  "devDependencies": {}
}